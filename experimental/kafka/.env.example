# Optional post-capture export only. The stable serving producer does not read
# these values and does not publish to Kafka.
PIQ_KAFKA_BOOTSTRAP_SERVERS=kafka-1:9092,kafka-2:9092
PIQ_KAFKA_CLIENT_ID=performance-iq-serving-exporter
PIQ_KAFKA_TOPIC=performance-iq.serving.telemetry.v1
